<h1>Listing business_segments</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Disable</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @business_segments.each do |business_segment| %>
  <tr>
    <td><%= business_segment.name %></td>
    <td><%= business_segment.enabled %></td>
    <td><%= link_to 'Show', business_segment %></td>
    <td><%= link_to 'Edit', edit_business_segment_path(business_segment) %></td>
    <td><%= link_to 'Destroy', business_segment, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Business segment', new_business_segment_path %>
