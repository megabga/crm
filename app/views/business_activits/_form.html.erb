<%= form_for(@business_activit) do |f| %>
  <% if @business_activit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business_activit.errors.count, "error") %> prohibited this business_activit from being saved:</h2>

      <ul>
      <% @business_activit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :disable %><br />
    <%= f.check_box :disable %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
